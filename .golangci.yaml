run:
  tests: false

linters:
  enable:
    - revive

issues:
  exclude-rules:
    - linters:
        - govet
      text: "OperationOutcome"
    - linters:
        - golint
      text: "type name will be used"
